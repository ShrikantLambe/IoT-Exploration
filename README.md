Updated README with LWA steps and sample usage.

- Added instructions to set `ALEXA_CLIENT_ID`, `ALEXA_CLIENT_SECRET`, and `ALEXA_REDIRECT_URI`.
- Documented how to start OAuth flow and call `/send` after authorizing.

